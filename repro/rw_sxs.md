# Related-work rephrase side-by-side (Q20 cone, 7 paragraphs)

Merged 4 clean, 3 with reattached headers. All numbers and cite keys verified identical.

## RW:0

Mine:

\paragraph{Single directions: the starting point we outgrew.}
Refusal can be steered with one residual direction~\cite{arditi2024refusal},
constructed as a mean difference over contrastive prompts. We keep that
construction for our own directions and keep rank one itself only as an
appendix baseline, because every strand below warns against stopping there.

Model:

The starting point that we outgrew is single directions; refusal can be steered with one residual direction~\cite{arditi2024refusal}, constructed as a mean difference over contrastive prompts. We keep that construction for our own directions and keep rank one itself only as an appendix baseline, because every strand below warns against stopping there.

## RW:1

Mine:

\paragraph{Cones: the working object.}
The behavior behind a single steerable direction spans cones and
manifolds~\cite{wollschlager2025cones}, and orthogonality alone does not imply
independence under intervention. Where refusal needed a cone rather than a
vector, writing gets the same upgrade: our object is a positive-part cone,
$h - \alpha B\max(B^T(h-\mu),0)$, not a subspace. The clamp is the point;
without it the intervention removes whatever the projection catches, and the
falsification suite shows that version fails.

Model:

\paragraph{Cones: the working object.} The behavior behind a single steerable direction spans cones and manifolds~\cite{wollschlager2025cones}, and orthogonality alone does not imply independence under intervention. Where refusal required a cone rather than a vector, writing gets the same upgrade: our object is a positive-part cone, $h - \alpha B\max(B^T(h-\mu),0)$, not a subspace. The clamp is the point; without it, the intervention removes whatever the projection catches, and the falsification suite shows that version fails.

## RW:4

Mine:

\paragraph{Narrative signal: why low rank at all.}
Authorship signal spreads redundantly across discourse
dimensions~\cite{russell2026storyscope}. A phenomenon smeared across
dimensions argues against rank one from the data side, exactly as the geometry
argues from the mechanism side. Both point the same way, which is why the
study starts at low rank with an appendix baseline rather than at rank one
with ambitions.

Model:

Narrative signal: why low rank at all. Authorship signal spreads redundantly across discourse dimensions~\cite{russell2026storyscope}. A phenomenon smeared across dimensions argues against rank one from the data side, exactly as the geometry argues from the mechanism side. Both point the same way, which is why the study starts at low rank with an appendix baseline rather than at rank one with ambitions.

